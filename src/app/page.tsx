import UIFooter from './ui/Footer';
import Navbar from './ui/Navbar';
import { Layout } from 'antd';

const Home = () => {
  return (
    <Layout>
      <Navbar />
      <UIFooter />
    </Layout>
  );
};

export default Home;
